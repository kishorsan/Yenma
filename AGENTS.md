# Project scope preference

The user requested on 2026-09-12 that work focus purely on the Flutter application. Do not inspect or modify Kotlin code, Kotlin Gradle scripts, or the separate Kotlin Money application unless the user explicitly changes this instruction. Describe platform integrations from their Dart interfaces and mark native behavior as unverified when needed.

For feature documentation, use current Flutter source as evidence, including working-tree changes. Distinguish existing behavior, inferred user benefits, unresolved product intent, and planned features. The older docs/money-mobile-application reference inventory describes another application and must not be treated as the current Flutter feature list.

Current Flutter feature/use-case inventory: docs/flutter-features.md.
